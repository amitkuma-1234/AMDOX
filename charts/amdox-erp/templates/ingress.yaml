{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "amdox.fullname" . }}
  namespace: {{ include "amdox.namespace" . }}
  labels:
    {{- include "amdox.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- range .Values.ingress.tls }}
    - secretName: {{ .secretName }}
      hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                {{- if eq .service "api" }}
                name: {{ include "amdox.fullname" $ }}-api
                port:
                  number: {{ $.Values.api.port }}
                {{- else if eq .service "web" }}
                name: {{ include "amdox.fullname" $ }}-web
                port:
                  number: {{ $.Values.web.port }}
                {{- else if eq .service "ml-service" }}
                name: {{ include "amdox.fullname" $ }}-ml-service
                port:
                  number: {{ $.Values.mlService.port }}
                {{- end }}
          {{- end }}
    {{- end }}
{{- end }}
