# ============================================================
# AMDOX Release Pipeline
# ============================================================
# Merged into deploy.yml — this file retained for backward compat.
# See deploy.yml for the full staging → production pipeline.
# ============================================================

name: Release

on:
  push:
    tags: ["v*"]

env:
  NODE_VERSION: "22"
  PNPM_VERSION: "9.15.4"

jobs:
  # Delegate to deploy pipeline
  redirect:
    name: "See deploy.yml"
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release pipeline moved to deploy.yml. This workflow triggers automatically on v* tags."
