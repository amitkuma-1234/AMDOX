# ============================================================
# Kustomize Base — AMDOX ERP
# ============================================================
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: amdox

resources:
  - ../../k8s/namespaces.yaml
  - ../../k8s/rbac.yaml

# Helm chart is the primary deployment method.
# This kustomize base provides raw K8s manifests as an alternative.
# Use overlays to patch per environment.

commonLabels:
  app.kubernetes.io/part-of: amdox-erp
  app.kubernetes.io/managed-by: kustomize
